# Generated flake.nix for RingTheory.SimpleModule.IsAlgClosed
{
  description = "Mathlib declaration: RingTheory.SimpleModule.IsAlgClosed";
  inputs = {
    nixpkgs.url = "github:NixOS/nixpkgs/nixos-unstable";
    flake-utils.url = "github:numtide/flake-utils";
  };
  outputs = { self, nixpkgs, flake-utils }:
    let
      system = "x86_64-linux";
      pkgs = nixpkgs.legacyPackages.${system};
    in {
      packages.${system}.default = pkgs.stdenv.mkDerivation {
        pname = "decl-RingTheory.SimpleModule.IsAlgClosed";
        version = "0.1.0";
        src = ./.;
        phases = [ "unpackPhase" "installPhase" ];
        installPhase = ''
          mkdir -p $out
          cp RingTheory/SimpleModule/IsAlgClosed.lean $out/
        '';
      };
    };
}
