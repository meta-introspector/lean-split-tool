# Generated flake.nix for Order.SuccPred.LinearLocallyFinite
{
  description = "Mathlib declaration: Order.SuccPred.LinearLocallyFinite";
  inputs = {
    nixpkgs.url = "github:NixOS/nixpkgs/nixos-unstable";
    flake-utils.url = "github:numtide/flake-utils";
  };
  outputs = { self, nixpkgs, flake-utils }:
    let
      system = "x86_64-linux";
      pkgs = nixpkgs.legacyPackages.${system};
    in {
      packages.${system}.default = pkgs.stdenv.mkDerivation {
        pname = "decl-Order.SuccPred.LinearLocallyFinite";
        version = "0.1.0";
        src = ./.;
        phases = [ "unpackPhase" "installPhase" ];
        installPhase = ''
          mkdir -p $out
          cp Order/SuccPred/LinearLocallyFinite.lean $out/
        '';
      };
    };
}
