# Generated flake.nix for CategoryTheory.Sites.DenseSubsite.SheafEquiv
{
  description = "Mathlib declaration: CategoryTheory.Sites.DenseSubsite.SheafEquiv";
  inputs = {
    nixpkgs.url = "github:NixOS/nixpkgs/nixos-unstable";
    flake-utils.url = "github:numtide/flake-utils";
  };
  outputs = { self, nixpkgs, flake-utils }:
    let
      system = "x86_64-linux";
      pkgs = nixpkgs.legacyPackages.${system};
    in {
      packages.${system}.default = pkgs.stdenv.mkDerivation {
        pname = "decl-CategoryTheory.Sites.DenseSubsite.SheafEquiv";
        version = "0.1.0";
        src = ./.;
        phases = [ "unpackPhase" "installPhase" ];
        installPhase = ''
          mkdir -p $out
          cp CategoryTheory/Sites/DenseSubsite/SheafEquiv.lean $out/
        '';
      };
    };
}
