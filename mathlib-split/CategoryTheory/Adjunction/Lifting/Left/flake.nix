# Generated flake.nix for CategoryTheory.Adjunction.Lifting.Left
{
  description = "Mathlib declaration: CategoryTheory.Adjunction.Lifting.Left";
  inputs = {
    nixpkgs.url = "github:NixOS/nixpkgs/nixos-unstable";
    flake-utils.url = "github:numtide/flake-utils";
  };
  outputs = { self, nixpkgs, flake-utils }:
    let
      system = "x86_64-linux";
      pkgs = nixpkgs.legacyPackages.${system};
    in {
      packages.${system}.default = pkgs.stdenv.mkDerivation {
        pname = "decl-CategoryTheory.Adjunction.Lifting.Left";
        version = "0.1.0";
        src = ./.;
        phases = [ "unpackPhase" "installPhase" ];
        installPhase = ''
          mkdir -p $out
          cp CategoryTheory/Adjunction/Lifting/Left.lean $out/
        '';
      };
    };
}
