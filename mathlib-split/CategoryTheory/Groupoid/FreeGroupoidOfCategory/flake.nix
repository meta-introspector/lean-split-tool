# Generated flake.nix for CategoryTheory.Groupoid.FreeGroupoidOfCategory
{
  description = "Mathlib declaration: CategoryTheory.Groupoid.FreeGroupoidOfCategory";
  inputs = {
    nixpkgs.url = "github:NixOS/nixpkgs/nixos-unstable";
    flake-utils.url = "github:numtide/flake-utils";
  };
  outputs = { self, nixpkgs, flake-utils }:
    let
      system = "x86_64-linux";
      pkgs = nixpkgs.legacyPackages.${system};
    in {
      packages.${system}.default = pkgs.stdenv.mkDerivation {
        pname = "decl-CategoryTheory.Groupoid.FreeGroupoidOfCategory";
        version = "0.1.0";
        src = ./.;
        phases = [ "unpackPhase" "installPhase" ];
        installPhase = ''
          mkdir -p $out
          cp CategoryTheory/Groupoid/FreeGroupoidOfCategory.lean $out/
        '';
      };
    };
}
