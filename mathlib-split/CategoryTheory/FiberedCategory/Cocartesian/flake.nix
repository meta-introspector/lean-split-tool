# Generated flake.nix for CategoryTheory.FiberedCategory.Cocartesian
{
  description = "Mathlib declaration: CategoryTheory.FiberedCategory.Cocartesian";
  inputs = {
    nixpkgs.url = "github:NixOS/nixpkgs/nixos-unstable";
    flake-utils.url = "github:numtide/flake-utils";
  };
  outputs = { self, nixpkgs, flake-utils }:
    let
      system = "x86_64-linux";
      pkgs = nixpkgs.legacyPackages.${system};
    in {
      packages.${system}.default = pkgs.stdenv.mkDerivation {
        pname = "decl-CategoryTheory.FiberedCategory.Cocartesian";
        version = "0.1.0";
        src = ./.;
        phases = [ "unpackPhase" "installPhase" ];
        installPhase = ''
          mkdir -p $out
          cp CategoryTheory/FiberedCategory/Cocartesian.lean $out/
        '';
      };
    };
}
