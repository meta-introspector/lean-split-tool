# Generated flake.nix for Algebra.Order.GroupWithZero.Action.Synonym
{
  description = "Mathlib declaration: Algebra.Order.GroupWithZero.Action.Synonym";
  inputs = {
    nixpkgs.url = "github:NixOS/nixpkgs/nixos-unstable";
    flake-utils.url = "github:numtide/flake-utils";
  };
  outputs = { self, nixpkgs, flake-utils }:
    let
      system = "x86_64-linux";
      pkgs = nixpkgs.legacyPackages.${system};
    in {
      packages.${system}.default = pkgs.stdenv.mkDerivation {
        pname = "decl-Algebra.Order.GroupWithZero.Action.Synonym";
        version = "0.1.0";
        src = ./.;
        phases = [ "unpackPhase" "installPhase" ];
        installPhase = ''
          mkdir -p $out
          cp Algebra/Order/GroupWithZero/Action/Synonym.lean $out/
        '';
      };
    };
}
