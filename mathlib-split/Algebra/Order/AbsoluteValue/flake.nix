# Generated flake.nix for Algebra.Order.AbsoluteValue.Euclidean
{
  description = "Mathlib declaration: Algebra.Order.AbsoluteValue.Euclidean";
  inputs = {
    nixpkgs.url = "github:NixOS/nixpkgs/nixos-unstable";
    flake-utils.url = "github:numtide/flake-utils";
  };
  outputs = { self, nixpkgs, flake-utils }:
    let
      system = "x86_64-linux";
      pkgs = nixpkgs.legacyPackages.${system};
    in {
      packages.${system}.default = pkgs.stdenv.mkDerivation {
        pname = "decl-Algebra.Order.AbsoluteValue.Euclidean";
        version = "0.1.0";
        src = ./.;
        phases = [ "unpackPhase" "installPhase" ];
        installPhase = ''
          mkdir -p $out
          cp Euclidean.lean $out/
        '';
      };
    };
}
