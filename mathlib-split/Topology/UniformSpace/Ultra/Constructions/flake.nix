# Generated flake.nix for Topology.UniformSpace.Ultra.Constructions
{
  description = "Mathlib declaration: Topology.UniformSpace.Ultra.Constructions";
  inputs = {
    nixpkgs.url = "github:NixOS/nixpkgs/nixos-unstable";
    flake-utils.url = "github:numtide/flake-utils";
  };
  outputs = { self, nixpkgs, flake-utils }:
    let
      system = "x86_64-linux";
      pkgs = nixpkgs.legacyPackages.${system};
    in {
      packages.${system}.default = pkgs.stdenv.mkDerivation {
        pname = "decl-Topology.UniformSpace.Ultra.Constructions";
        version = "0.1.0";
        src = ./.;
        phases = [ "unpackPhase" "installPhase" ];
        installPhase = ''
          mkdir -p $out
          cp Topology/UniformSpace/Ultra/Constructions.lean $out/
        '';
      };
    };
}
