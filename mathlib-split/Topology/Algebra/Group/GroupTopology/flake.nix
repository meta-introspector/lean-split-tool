# Generated flake.nix for Topology.Algebra.Group.GroupTopology
{
  description = "Mathlib declaration: Topology.Algebra.Group.GroupTopology";
  inputs = {
    nixpkgs.url = "github:NixOS/nixpkgs/nixos-unstable";
    flake-utils.url = "github:numtide/flake-utils";
  };
  outputs = { self, nixpkgs, flake-utils }:
    let
      system = "x86_64-linux";
      pkgs = nixpkgs.legacyPackages.${system};
    in {
      packages.${system}.default = pkgs.stdenv.mkDerivation {
        pname = "decl-Topology.Algebra.Group.GroupTopology";
        version = "0.1.0";
        src = ./.;
        phases = [ "unpackPhase" "installPhase" ];
        installPhase = ''
          mkdir -p $out
          cp Topology/Algebra/Group/GroupTopology.lean $out/
        '';
      };
    };
}
